const calculateScore = (player) => {
  switch (player.position) {
    case 'QB':
      return (
      )
    case 'RB':
      return (
      )
    case 'WR':
      return (
       
      )

    case 'TE':
      return (
      )
    case 'K':
      return (0)
  }
}

module.exports = calculateScore
